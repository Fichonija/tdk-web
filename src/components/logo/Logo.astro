---
export interface Props {
  width: number;
}
const { width } = Astro.props as Props;

const { default: innerHTML } = await import(`./logo-white.svg?raw`);
---

<div style=`width: ${width}px`><Fragment set:html={innerHTML} /></div>
