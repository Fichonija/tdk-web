// when adding a new icon, map .svg file to IconTypes
// eg. `logo.svg` => IconType = ... | 'logo'
// in the svg file set width, height = 100%, and fill = currentColor

export type IconType = 'arrowRight' | 'logo' | 'logoText' | 'send' | 'telephone' | 'material' | 'cover';
